public class Car extends Vehicle{
    public Car(String color, int numberOfPassenger, int cargoCapacity, int fuelCapacity ){
        super(color, numberOfPassenger, cargoCapacity, fuelCapacity);
    }
}
